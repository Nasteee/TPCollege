{% extends "base.twig" %}

{% block title %}
    Classes
{% endblock %}

{% block container %}

    <div class="row">
        {% for classe in classes %}

            <div class="card col-md-2" >
                <div class="card-body">
                    <h5 class="card-title">{{ classe.nom }}</h5>
                    <a href="infosclasse.php?id={{ classe.id }}" class="btn btn-primary">Plus d'infos</a>
                </div>
            </div>

        {% endfor %}
    </div>



{% endblock %}